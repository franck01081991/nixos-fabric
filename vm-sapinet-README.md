# vm-sapinet Configuration - Pure Spine Node

**NixOS Fabric Infrastructure - Pure Spine Router Configuration**

## 🎯 Node Overview

**vm-sapinet** is a **pure spine node** in the hybrid network fabric architecture:

- **Role**: Core routing infrastructure
- **Protocols**: OSPF + BGP (IPv4 + IPv6)
- **Topology**: Full mesh connectivity
- **Function**: Core routing backbone

## 📂 Configuration Structure

```
vm-sapinet-config/
├── hardware-configuration.nix  # Hardware-specific settings (generated)
├── base-variables.nix         # Base configuration overrides
├── role-variables.nix          # Spine role variables
├── variables.nix              # Comprehensive configuration
└── default.nix                # Host-specific tweaks (15-20 lines)
```

## 🔧 Configuration Files

### `hardware-configuration.nix`
- **Generated by**: `nixos-generate-config`
- **Contains**: Hardware-specific settings (disks, filesystems, boot)
- **Do not edit manually** unless necessary

### `base-variables.nix`
- **Purpose**: Base system configuration overrides
- **Content**: Users, packages, system services, time zone

### `role-variables.nix`
- **Purpose**: Spine role-specific configuration
- **Content**: OSPF, BGP, routing policies

### `variables.nix`
- **Purpose**: Comprehensive configuration variables
- **Content**: Network interfaces, WireGuard, FRR, security

### `default.nix`
- **Purpose**: Minimal host-specific configuration
- **Content**: Host-specific tweaks (15-20 lines max)

## 🚀 Usage

### With NixOS Fabric Main Repository

This repository is designed to be used as a **Git submodule** in the main [nixos-fabric](https://github.com/franck01081991/nixos-fabric) repository.

### Standalone Usage

You can also use this configuration independently.

## 🔄 Update Process

```bash
# Make changes to configuration files
nano variables.nix

# Test locally
nix eval .#nixosConfigurations.vm-sapinet.config.networking.hostName

# Commit and push
git add .
git commit -m "Update vm-sapinet configuration"
git push origin main
```

## 📖 Related Documentation

- [Main Repository](https://github.com/franck01081991/nixos-fabric)
- [Deployment Guide](https://github.com/franck01081991/nixos-fabric/blob/master/docs/deployment/DEPLOYMENT.md)
- [Structure Reference](https://github.com/franck01081991/nixos-fabric/blob/master/docs/reference/STRUCTURE.md)

## 🛡️ Security

- WireGuard keys should be generated per-host
- BGP neighbors should use MD5 authentication
- SSH access should be restricted

## 📜 License

MIT License.